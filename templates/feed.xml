<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>{{ config.title }}</title>
  <id>{{ config.base_url | safe }}/</id>
  <link href="{{ config.base_url | safe }}/" />
  <link href="{{ feed_url | safe }}" rel="self" type="application/atom+xml" />
  <updated>{{ last_updated | date(format="%Y-%m-%dT%H:%M:%SZ") }}</updated>
  {% if config.description %}<subtitle>{{ config.description }}</subtitle>{% endif %}
  {%- set_global emitted = 0 %}
  {%- for page in pages %}
  {%- if page.components | length == 2 and page.components.0 == "digests" %}
  <entry>
    <title>{{ page.title }}</title>
    <id>{{ page.permalink | safe }}</id>
    <link href="{{ page.permalink | safe }}" />
    <updated>{% if page.updated %}{{ page.updated | date(format="%Y-%m-%dT%H:%M:%SZ") }}{% else %}{{ page.date | date(format="%Y-%m-%dT%H:%M:%SZ") }}{% endif %}</updated>
    {% if page.description %}<summary>{{ page.description }}</summary>{% endif %}
  </entry>
  {%- set_global emitted = emitted + 1 %}
  {%- if emitted >= 30 %}{% break %}{% endif %}
  {%- endif %}
  {%- endfor %}
</feed>
