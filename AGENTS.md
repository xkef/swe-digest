# Agent instructions

Read `CLAUDE.md` first. It is the canonical routine for this repository.

Use `routine/routine.md` for the detailed source checklist, `routine/watchlist.toml` for maintained interests, and `memory/` for public long-running context.

Do not publish private context. Do not commit `PRIVATE_CONTEXT.md`, `dist/`, `public/`, or local caches.
