"""Evidence built from what the store already holds.

The backtest scores a published day against the sources it could have used, and
the weekly aggregation turns a window of run logs into the numbers the
improvement run reasons about. Both read; neither decides.
"""
