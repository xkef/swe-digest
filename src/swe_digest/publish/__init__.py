"""Turning the day's digest into what the site builds from.

The skeleton the write step fills in, the canonical form the gate expects, and
the Zola content tree the build consumes. The only writer of ``site/content/``.
"""
